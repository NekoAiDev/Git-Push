# Python
__pycache__/
*.pyc
*.pyo

# WorkBuddy 会话数据
.workbuddy/

# PyInstaller 打包产物（dist/GitPush.exe 另有提交，此处仅忽略中间文件）
build/
# 注意：GitPush.spec 是手工维护的打包配方，不是自动生成的中间文件，需要跟踪；
# 如果以后生成其他临时 spec 备份，再单独加规则（如 *.spec.bak）。

# 本地自签名证书指纹（仅本机工作用，不入库）
cert_thumbprint.txt

# 构建过程日志（本机产物，不入库）
build_log.txt
cert_log.txt
iscc_log.txt
iscc_out.txt
iscc_err.txt
updatezip_log.txt

# 打包过程产生的临时 zip
public.zip

# Wrangler 本地产物
.wrangler/

# 工具运行时本地匿名统计配置（用户本机生成，含随机 UUID/计数，属隐私数据不入库）
dist/stats.json
